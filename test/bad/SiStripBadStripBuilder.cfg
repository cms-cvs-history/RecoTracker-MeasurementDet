process ICALIB = {
    
    service = MessageLogger {
	untracked vstring destinations = { "cout" }
	untracked PSet cout = { untracked string threshold = "INFO" }
    }

    source = EmptyIOVSource {
	string timetype = "runnumber"
        uint64 firstValue = 1
	uint64 lastValue = 1
        uint64 interval = 1
    }
    untracked PSet maxEvents = {untracked int32 input = 1}
  
    
    service = PoolDBOutputService{
	string connect = "sqlite_file:dbfile-badstrip.db"    
	untracked string timetype = "runnumber"    
	untracked string BlobStreamerName="DefaultBlobStreamingService"
	PSet DBParameters = {
	    untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
	}
	
        VPSet toPut={ { string record = "SiStripBadStrip" string tag = "SiStripBadChannel_v1"} }
    }
        
    module prod =  SiStripBadChannelBuilder {

	untracked bool   printDebug         = true
	untracked FileInPath file = "CalibTracker/SiStripCommon/data/SiStripDetInfo.dat"

	untracked VPSet BadComponentList = {
	    { uint32 BadModule = 470178036 vuint32 BadChannelList = {4,5,6,7,8,9, 511,512, 650} } ,
	    { uint32 BadModule = 470178032 vuint32 BadChannelList = {4,5,6,7,8,9, 378,379,380, 511,512, 650} } ,
	    { uint32 BadModule = 470178024 vuint32 BadChannelList = {4,5,6,7,8,9, 127,128, 378,379,380, 511,512, 650} }
	}

	bool SinceAppendMode = true
	string IOVMode	     = "Run"
	string Record        = "SiStripBadStrip"
	bool doStoreOnDB     = true
    }

    include "RecoTracker/MeasurementDet/test/bad/BadStrips.cff"

    path p = { prod }
        
    module print = AsciiOutputModule {}
    endpath ep = { print }
}

